let firstName = prompt('What is your given name?');

let secondName = prompt('What is your other name?');

let fullName = `Welcome,${firstName} ${secondName} thanks for visiting our website`;
alert(fullName);